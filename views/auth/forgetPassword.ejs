<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Quên Mật Khẩu</title>
</head>

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-sm-10 col-md-8 col-lg-6">
                <h2 class="bg-danger rounded-top p-2 ps-4 text-white mb-0 fw-bold">Quên mật khẩu</h2>
                <form action="./forget-password" method="post" class="border border-top-0 rounded-bottom bg-light p-3">
                    <label for="EoP" class="form-label">Nhập địa chỉ email hoặc số điện thoại</label>
                    <input type="text" class="form-control" id="EoP" name="EoP">
                    <!-- Email or Phone dùng để tìm lại mật khẩu -->
                    <br>
                    <div class="mb-3 d-flex justify-content-center">
                        <% if (messages.error) { %>
                            <p style="color:red">
                                <%- messages.error %>
                            </p>
                            <% } %>

                                <% if (messages.success) { %>
                                    <p style="color:green">
                                        <%- messages.success %>
                                    </p>
                                    <% } %>

                    </div>

                    <div class="d-flex justify-content-center">

                        <button type="submit" class="btn btn-danger fw-bold">Xác nhận</button>

                    </div>

                </form>
                <div class="d-flex justify-content-center">
                    <button onclick="window.location.href='/'" class="btn btn-success mb-4 fw-bold">Trang chủ</button>
                </div>
            </div>
        </div>
    </div>
</body>

</html>