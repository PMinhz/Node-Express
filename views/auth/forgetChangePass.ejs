<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Đổi mật khẩu</title>
</head>

<body>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-sm-10 col-md-8 col-lg-6 col-xl-4">
                <h2 class="bg-warning rounded-top p-2 ps-4 text-white mb-0 fw-bold">Đổi mật khẩu</h2>
                <form action="./forgetChangePass" method="Post" class="border border-top-0 rounded-bottom bg-light p-3">
                    <div class="mb-3">
                        <label for="Email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" name="email">
                    </div>
                    <div class="mb-3">
                        <label for="code" class="form-label">Code</label>
                        <input type="int" class="form-control" id="code" name="code">
                    </div>
                    <div class="mb-3">
                        <label for="newPwd" class="form-label">Mật khẩu mới</label>
                        <input type="password" class="form-control" id="newPwd" name="pswd1">
                    </div>
                    <div class="mb-3">
                        <label for="confirmPwd" class="form-label">Xác nhận mật khẩu mới</label>
                        <input type="password" class="form-control" id="confirmPwd" name="pswd2">
                    </div>
                    <div class="mb-3 d-flex justify-content-center">
                        <% if (messages.error) { %>
                            <p style="color:red">
                                <%- messages.error %>
                            </p>
                            <% } %>

                                <% if (messages.success) { %>
                                    <p style="color:green">
                                        <%- messages.success %>
                                    </p>
                                    <% } %>
                    </div>
                    <div class="d-flex justify-content-center">
                        <button type="submit" class="btn btn-warning mb-4 fw-bold">Lưu</button>

                    </div>
                </form>
                <div class="d-flex justify-content-center">
                    <button onclick="window.location.href='/'" class="btn btn-success mb-4 fw-bold">Trang chủ</button>
                </div>
            </div>
        </div>
    </div>
</body>

</html>